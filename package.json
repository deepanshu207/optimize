{
  "name": "optimize",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "python3 server.py",
    "deploy": "cp deploy/_headers app.suppliersden.com/_headers && wrangler deploy; status=$?; rm -f app.suppliersden.com/_headers; exit $status",
    "build:extension": "bash scripts/build-extension-zip.sh",
    "verify:deploy": "node scripts/verify-deploy.mjs",
    "build:categories": "node scripts/build-meesho-categories.mjs",
    "test:live-report": "node scripts/test-live-variant-report.mjs",
    "test:category-pricing": "node scripts/test-category-generate-gate.mjs",
    "test:category-search": "node scripts/test-category-search.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.14.0"
  },
  "dependencies": {
    "puppeteer-core": "^25.4.0"
  }
}
