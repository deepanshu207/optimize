{
  "name": "optimize",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.0.0" },
  "scripts": {
    "dev": "python3 server.py",
    "deploy": "wrangler deploy",
    "verify:deploy": "node scripts/verify-deploy.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.14.0"
  }
}
